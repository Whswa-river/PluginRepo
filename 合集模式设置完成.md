# 合集模式设置完成

## 已完成的配置

### 1. 合集仓库结构
```
D:\zjc\FF14\dalamud\PluginRepo\
├── plugins.json          # 合集配置文件
├── README.md             # 仓库说明
└── .gitignore            # Git 忽略文件
```

### 2. 合集 JSON 内容
`plugins.json` 包含两个插件：
- **RiverBox** (1.0.8.0)
- **AutomaticHunting** (1.0.0.0)

### 3. 发布脚本更新
- `release.py` (RiverBox) - 自动更新合集 JSON
- `release_AutomaticHunting.py` - 自动更新合集 JSON

## 工作流程

### 发布新版本时
1. 运行插件的发布脚本（如 `python release.py`）
2. 脚本自动：
   - 编译插件
   - 创建 GitHub Release
   - 更新插件的 JSON 文件
   - **更新合集仓库的 `plugins.json`**
   - 推送两个仓库的更改

### 用户安装插件
只需添加一个仓库 URL：
```
https://raw.githubusercontent.com/Whswa-river/PluginRepo/main/plugins.json
```

## 仓库结构对比

### 单插件仓库 (Whswa-river/River)
```
├── RiverBox/
│   ├── RiverBox.csproj
│   └── ...
├── AutomaticHunting/
│   ├── AutomaticHunting.csproj
│   └── ...
├── RiverBox.json          # 单插件配置（可选保留）
├── release.py
└── release_AutomaticHunting.py
```

### 合集仓库 (Whswa-river/PluginRepo)
```
├── plugins.json           # 所有插件的配置
├── README.md
└── .gitignore
```

## 下一步操作

### 1. 创建 GitHub 仓库
在 GitHub 上创建新仓库 `Whswa-river/PluginRepo`

### 2. 初始化本地仓库
```bash
cd D:\zjc\FF14\dalamud\PluginRepo
git init
git remote add origin https://github.com/Whswa-river/PluginRepo.git
git add .
git commit -m "Initial commit: plugin collection"
git push -u origin main
```

### 3. 测试发布
```bash
# 测试 RiverBox 发布
cd D:\zjc\FF14\dalamud\box
python release.py

# 测试 AutomaticHunting 发布
cd D:\zjc\FF14\dalamud\AutomaticHunting\独立插件
python release_AutomaticHunting.py
```

## 优势

1. **统一安装入口** - 用户只需添加一个仓库 URL
2. **独立版本管理** - 每个插件独立发布和版本控制
3. **自动同步** - 发布脚本自动更新合集配置
4. **易于扩展** - 添加新插件只需在合集 JSON 中添加条目

## 添加新插件

要添加新插件到合集：
1. 创建插件的发布脚本（参考现有脚本）
2. 在 `plugins.json` 中添加新条目
3. 运行发布脚本

## 故障排除

### 合集 JSON 未更新
- 检查 `COLLECTION_JSON_PATH` 路径是否正确
- 确保 `PluginRepo` 目录存在

### 推送失败
- 检查 GitHub Token 权限
- 确保两个仓库都已正确配置远程地址